{
  "name": "openstack-k8s-agent-tools",
  "version": "0.1.0",
  "description": "Development and troubleshooting tools for openstack-k8s-operators operators",
  "main": "index.js",
  "scripts": {
    "test": "echo \"No tests yet\" && exit 0",
    "lint": "echo \"No linting configured yet\" && exit 0",
    "format": "echo \"No formatting configured yet\" && exit 0",
    "build": "echo \"No build step required\" && exit 0",
    "install:claude": "./scripts/install.sh --claude-code",
    "install:opencode": "./scripts/install.sh --opencode",
    "check": "./scripts/install.sh --check"
  },
  "keywords": [
    "openstack-k8s-operators",
    "operators",
    "debugging",
    "kubernetes",
    "openstack",
    "claude-code",
    "plugin"
  ],
  "author": "openstack-k8s-operators Development Team",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {},
  "repository": {
    "type": "git",
    "url": "https://github.com/openstack-k8s-operators/operator-tools.git"
  },
  "bugs": {
    "url": "https://github.com/openstack-k8s-operators/operator-tools/issues"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}